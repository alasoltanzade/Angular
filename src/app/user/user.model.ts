export interface User {
  id: string;
  avatar: string;
  name: string;
} //so now we can use it in every file

export interface Car {
  id: string;
  wheel: string;
  glass: string;
  initial_price: number;
  new_price: number;
}
